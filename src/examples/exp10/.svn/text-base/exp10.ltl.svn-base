
-- LTL specification file
-- (Generated by the LTLMoP toolkit)

LTLSPEC -- Assumptions
	(
			 !e.tempNewRegion & 
			 !e.hazardous & 
			 !e.assignment & 
			 !e.newOffice & 
			 !e.detectPOI & 
			 !e.gotoPOIDone & 
			 [](  !  (next(e.tempNewRegion)  &  next(e.newOffice))) & 
			[]<>(TRUE)
	);

LTLSPEC -- Guarantees
	(
			( (!s.bit0 & !s.bit1) 
				 | (!s.bit0 & s.bit1)
				 | (s.bit0 & !s.bit1)
				 | (s.bit0 & s.bit1)
			) & 
			 !s.resynthesize & 
			 !s.flag & 
			 !s.gotoPOI & 
			 []( ((!s.bit0 & !s.bit1)) -> ( ((!next(s.bit0) & !next(s.bit1)))
									| ((!next(s.bit0) & next(s.bit1)))  ) ) & 
 			 []( ((!s.bit0 & s.bit1)) -> ( ((!next(s.bit0) & next(s.bit1)))
									| ((!next(s.bit0) & !next(s.bit1))) 
									| ((next(s.bit0) & !next(s.bit1)))  ) ) & 
 			 []( ((s.bit0 & !s.bit1)) -> ( ((next(s.bit0) & !next(s.bit1)))
									| ((!next(s.bit0) & next(s.bit1))) 
									| ((next(s.bit0) & next(s.bit1)))  ) ) & 
 			 []( ((s.bit0 & s.bit1)) -> ( ((next(s.bit0) & next(s.bit1)))
									| ((next(s.bit0) & !next(s.bit1)))  ) ) & 
 			 ([](( (next(e.detectPOI)) & !(next(e.gotoPOIDone))) -> next(s.gotoPOI)) ) & 
			 ([](  (next(e.gotoPOIDone)) -> !next(s.gotoPOI)) ) & 
			 ([](( (s.gotoPOI) & !(next(e.gotoPOIDone))) -> next(s.gotoPOI)) ) & 
			 ([](( !(s.gotoPOI) & !(next(e.detectPOI))) -> !next(s.gotoPOI)) ) & 
			 []((( s.gotoPOI )  |  ( next(s.gotoPOI) ) ) -> ( (next(s.bit0) <-> s.bit0) & (next(s.bit1) <-> s.bit1) )) & 
			 []((( next(e.hazardous) ) ) <-> ( next(s.flag)) ) & 
			 []((( next(e.tempNewRegion) ) ) -> ( next(s.resynthesize) ) ) & 
			 []((( next(e.newOffice) ) ) -> ( next(s.resynthesize) ) ) & 
			 []((( next(e.tempNewRegion) )  |  ( next(e.newOffice) ) ) -> ( (next(s.bit0) <-> s.bit0) & (next(s.bit1) <-> s.bit1) )) & 
			 []((!( next(e.tempNewRegion) )  &  !( next(e.newOffice) ) ) -> (  !  next(s.resynthesize)) ) & 
			 []<>((!( e.assignment )  &  !( s.resynthesize )  &  !( s.gotoPOI ) ) -> (  ((!s.bit0 & s.bit1))) ) & 
			 []<>((!( e.assignment )  &  !( s.resynthesize )  &  !( s.gotoPOI ) ) -> (  ((!s.bit0 & !s.bit1))) ) 

	);
